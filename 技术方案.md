# 技术方案文档

> 版本：v1.3　日期：2026-08-11　配套文档：《方案.md》《需求文档.md》《数据模型.md》
>
> MVP 范围：仅桌面版，无响应式。首页仅 Hero 开场；集锦页仅照片墙；弹幕用预置留言。
> 地图引擎：MapLibre GL + OpenFreeMap 矢量瓦片（真实地图，可缩放至街道级）。

本文档描述工程化细节：项目初始化、依赖选型、关键实现要点、部署流程与风险对策。

## 1. 环境要求

- Node.js ≥ 20（推荐 LTS 22）
- 包管理器：npm 或 pnpm（建议 pnpm，快且省空间）
- Git + GitHub 账号（用于部署与 Giscus）

## 2. 项目初始化

```bash
npm create vite@latest n4a-website -- --template react-ts
cd n4a-website
npm install
```

安装依赖：

```bash
npm install react-router-dom framer-motion
npm install gray-matter          # 解析 Markdown frontmatter
npm install maplibre-gl          # 矢量地图引擎
npm install yet-another-react-lightbox   # 图片灯箱（零依赖）
npm install @fontsource/noto-serif-sc @fontsource/noto-sans-sc  # 思源宋体/黑体（自托管）
```

> 说明：地图用原生 `maplibre-gl` 接入（ref + useEffect），不依赖 react 封装库，避免版本兼容问题。瓦片源用 OpenFreeMap（无 key）。

## 3. 目录结构规划

```
src/
├── main.tsx
├── App.tsx               # 路由表 + 布局（Navbar/Footer）
├── pages/
│   ├── HomePage.tsx
│   ├── MapPage.tsx
│   ├── VenueDetailPage.tsx
│   ├── GalleryPage.tsx
│   ├── GuestbookPage.tsx
│   └── AchievementsPage.tsx
├── components/
│   ├── Navbar.tsx / Footer.tsx
│   ├── common/ (Lightbox.tsx)
│   ├── venue/ (VenueCard.tsx)
│   ├── map/ (MapView.tsx, VenueMarker.tsx, InfoCard.tsx, TimeSlider.tsx, FlyLines.tsx, DanmakuLayer.tsx)
│   ├── gallery/ (PhotoWall.tsx)
│   ├── guestbook/ (GiscusEmbed.tsx)
│   └── home/ (HeroSection.tsx)   # MVP 首页仅 Hero 开场
├── styles/
│   ├── theme.css          # CSS 变量（主色/间距/字体）
│   └── global.css
├── hooks/                 # useMapFocus、useViewport 等自定义 hook
├── lib/
│   └── content.ts         # 解析 content/ 下 md：import.meta.glob + gray-matter → 类型化数据
└── types/
    └── content.ts         # 内容 schema 的 TS 接口（校验 frontmatter）

content/                    # 内容结构化文档（Markdown + frontmatter），见《数据模型.md》
├── venues/*.md
├── timeline/*.md
├── events/*.md
├── visits/*.md
└── testimonials.md          # 弹幕蒙版预置留言

src/data/geo/                 # 地图几何数据（打包进项目，零外网依赖）
├── provinces.json            # 四省（鄂/赣/皖/苏）省界 GeoJSON（DataV GeoAtlas）
└── china.json                # 中国轮廓 GeoJSON（用于范围定位/背景，可选）
```

样式：每个组件一个 `*.module.css`；主题变量集中在 `styles/theme.css` 的 `:root`。

## 4. 路由规划

```ts
// App.tsx 路由表（React Router v6）
<Routes>
  <Route path="/" element={<HomePage />} />
  <Route path="/map" element={<MapPage />} />
  <Route path="/venues/:id" element={<VenueDetailPage />} />
  <Route path="/gallery" element={<GalleryPage />} />
  <Route path="/guestbook" element={<GuestbookPage />} />
  <Route path="/achievements" element={<AchievementsPage />} />
  <Route path="*" element={<NotFound />} />
</Routes>
```

懒加载：除首页外全部 `React.lazy` + `Suspense`，实现路由级代码分割。

## 4.1 内容解析（结构化文档 → 类型化数据）

`content/` 下所有内容均为 Markdown + frontmatter 文档（见《数据模型.md》），在 `src/lib/content.ts` 统一解析：

```ts
import { glob } from 'glob';          // 或 Vite import.meta.glob
import matter from 'gray-matter';
import type { Venue, TimelinePeriod, HistoricalEvent, VisitRecord } from '../types/content';

const venueFiles = import.meta.glob('/content/venues/*.md', { eager: true, query: '?raw' });

export function loadVenues(): Venue[] {
  return Object.values(venueFiles).map((mod: any) => {
    const { data, content } = matter(mod.default);
    return parseVenue(data, content); // 按 schema 校验并转换
  });
}
```

要点：
- 构建期用 Vite `import.meta.glob` 打包加载，避免运行时请求
- 解析结果通过 `types/content.ts` 接口做类型断言，字段缺失/类型错误在开发期暴露
- 场馆详情正文用 `marked`（或轻量自定义渲染）转 HTML
- 事件解析：`content/events/*.md` **一馆一文件**，frontmatter 取 `venueId`，正文按 `## 日期 · 标题` 二级标题切分为多条事件（标题以 `·` 拆分出 date 与 title）

## 4.2 页面过渡动画

- 路由切换淡入淡出：在 `App.tsx` 布局中包一层 `Framer Motion` 的 `AnimatePresence` + `motion.div`（key 为 `location.pathname`），实现进出场淡入淡出
- 注意配合 `React.lazy`：`Suspense` fallback 保持淡出状态，避免闪白

## 4.3 图片灯箱

- 使用 `yet-another-react-lightbox`（零依赖、样式可定制）
- 封装为 `Lightbox.tsx` 组件，供场馆详情页图集与集锦页照片墙共用
- 数据形式：`{ src, alt }[]`，点击缩略图打开，支持键盘/左右切换/ESC 关闭

## 4.4 瀑布流照片墙

- 用 CSS `columns` 实现（`column-width` / `column-count` 响应式），无 JS 依赖
- 每张图片 `break-inside: avoid` 防止跨列
- 懒加载：`loading="lazy"` + 滚动到视口再加载（可选 IntersectionObserver）

## 4.5 字体引入

- `@fontsource/noto-serif-sc`（标题）与 `@fontsource/noto-sans-sc`（正文）自托管
- 在入口文件 `import '@fontsource/noto-serif-sc/400.css'` 等按需引入字重
- 仅引入用到的字重，避免打包过大（中文字体按需子集化由 fontsource 处理）

## 5. 地图实现要点（MapLibre GL）

### 5.1 地图初始化
- 原生 `maplibre-gl`：`new maplibregl.Map({ container: divRef, style, center, zoom })`，ref div 挂载
- 样式：OpenFreeMap 矢量瓦片 `https://tiles.openfreemap.org/styles/positron`（浅色基底），再叠加主题覆盖
  - 可选：用 `map.setPaintProperty` 调整底图配色（背景米黄、弱化水体/道路）融入红色主题
- 初始视野：`map.fitBounds(venueBounds, { padding })` 包住 8 场馆
- 无 key、无注册；矢量渲染，可缩放到街道级

### 5.2 标记点
- 数据：`GeoJSON source 'venues'`（8 个 Point，feature 带 `id`/`periodKey` 属性）
- 图层：`circle` 层（红色 `#B03A2E`）+ 可选 `symbol` 层（编号/名称）
- 时期区分：`circle-color` 用数据驱动或按时期设色；当前时期 `circle-radius` 放大 + 金色，非当前降 `circle-opacity`
- 脉冲动画：JS `requestAnimationFrame` 动态更新 `circle-radius`/`circle-opacity`（标准"pulsing marker"示例）
- 事件：`map.on('mouseenter'/'click')` + `queryRenderedFeatures` 命中场馆

### 5.3 悬停资料卡
- React 浮层组件（不依赖 mapbox 弹窗，便于 CSS Modules 定制）
- 位置：`map.project(coords)` 得屏幕坐标 → 定位浮层，处理视口边缘翻转
- 鼠标移开地图/卡片时隐藏

### 5.4 时间轴联动
- 全局状态：当前 `periodKey`（React Context）
- 改变时期：
  - `map.flyTo({ center: coords, zoom })` 平滑飞行
  - 图层 filter/属性高亮当前场馆，淡化其他
  - 侧栏显示 `TimelinePeriod.summary / spiritNote`
- 点击地图场馆也可反向设置时间轴
- 初始定位：读取 URL query `?period=<key>`，进入页面 flyTo

### 5.5 飞线动画
- 数据：`GeoJSON LineString` 集合（相邻场馆，控制点抬高生成弧形）
- 图层：`line` 层（深红/金）+ 动画 dash（`line-dasharray` 随 rAF 平移）实现流动效果；或 rAF 移动一个"光点" Point 图层
- 触发：
  - 进页自动按时间顺序播一遍完整迁移（汉口→…→梅园）
  - 拖动时间轴只播当前段
- 提供关闭/重播控制
- 方案 B 备选：用 `line-gradient` 或自定义 shader（二期再优化）

### 5.6 四省省界强调
- 数据：`src/data/geo/provinces.json`（DataV GeoAtlas 四省边界）作为 `GeoJSON source`
- 图层：`fill` 层（`#B03A2E` 约 12% 透明度）+ `line` 描边（`#7B241C` 半透明），强调鄂/赣/皖/苏四省

### 5.7 布局（MVP：仅桌面）
- 只做桌面版布局：左侧地图主体 + 右侧场馆列表侧栏；不做移动端适配/底部抽屉

## 6. Giscus 接入

前置条件：
1. 创建公开 GitHub 仓库（如 `team/n4a-website`）
2. 仓库 Settings → General → **启用 Discussions**
3. 安装 [Giscus App](https://github.com/apps/giscus) 到仓库
4. 在 [giscus.app](https://giscus.app) 配置并获取：`data-repo`、`data-repo-id`、`data-category`、`data-category-id`

组件用法（React）：
```tsx
<script
  src="https://giscus.app/client.js"
  data-repo="team/n4a-website"
  data-repo-id="..."
  data-category="Announcements"
  data-category-id="..."
  data-mapping="pathname"
  data-strict="0"
  data-reactions-enabled="1"
  data-emit-metadata="0"
  data-theme="light"
/>
```
> `data-mapping="pathname"` 使留言按页面路径分区，留言板页独享一个讨论串。

### 6.1 地图主页弹幕蒙版

弹幕数据来源（MVP 定稿：**团队预置留言**）：

1. **团队预置留言轮播**（MVP 采用）
   - 内容为结构化文档 `content/testimonials.md`（frontmatter `texts: string[]`，见《数据模型.md》§2.5）
   - 弹幕直接取预置留言循环播放，零外部依赖、稳定美观

2. GitHub 真实评论拉取（**二期再议**）
   - 用仓库 Discussions 查询评论（GraphQL + token 或受限的未认证请求）
   - 涉及速率限制（约 60 次/小时/IP），需缓存策略

弹幕 UI 实现要点：
- 独立浮层组件 `DanmakuLayer.tsx`，absolute 定位于地图容器之上
- 动画用 Framer Motion 或纯 CSS transform 平移（性能好、不阻塞）
- 弹幕条半透明（背景 rgba + 文字描边），`pointer-events: none` 保证不挡地图交互
- **默认关闭**：地图页角落"弹幕"开关按钮控制开启/关闭（按钮自身 `pointer-events: auto`）
- 提供暂停
- 样式归属 `DanmakuLayer.module.css`，融入红色主题

## 7. 图片压缩方案

目标：全部转 WebP，单张 ≤300KB，建议尺寸宽 ≤1600px（灯箱用原尺寸可另存）。

推荐工具（选一）：
- `npx squoosh-cli`（Google，命令行，批处理）
- `npx sharp-cli`（sharp，速度快，支持 WebP）
- 在线工具（少量图片时）

示例（sharp-cli 批处理）：
```bash
npx sharp-cli -i input/*.jpg -o output/ --format webp --quality 80 --resize 1600 0
```

压缩后放入 `public/images/venues/<id>/` 或 `public/images/visits/<地点>/`。

## 8. GitHub Pages 部署

### 8.1 SPA base 路径
Vite 构建需设置 base：
- 若部署到 `https://<user>.github.io/<repo>/`：`vite.config.ts` 中 `base: '/<repo>/'`
- 若用自定义域名根路径：`base: '/'`

### 8.2 部署方式
方式一（推荐）：GitHub Actions
```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci && npm run build
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```
Settings → Pages → Source: `gh-pages` branch。

方式二（手动）：`npm run build` 后把 `dist/` 推送到 `gh-pages` 分支。

### 8.3 SPA 路由 404 处理
GitHub Pages 不支持 SPA fallback。方案：
- 使用 **HashRouter**（`#/map` 形式，最省事，推荐本项目）
- 或添加 `404.html` 复制 `index.html`（需自定义脚本）

## 9. 待办与风险清单

| # | 风险/待办 | 等级 | 对策 |
|---|-----------|:---:|------|
| 1 | maplibre-gl 版本/API 变动 | 低 | 锁定版本；核心 API（Map/图层/flyTo）稳定 |
| 2 | Giscus 依赖 GitHub 仓库，未创建则无法展示 | 高 | 首期用占位块，待仓库就绪即接入 |
| 3 | 未实地场馆配图缺失 | 中 | 从官网/公开渠道获取，先占位后替换 |
| 4 | 云岭大图拖慢页面 | 高 | 必须压缩（见第 7 节） |
| 5 | SPA 部署 404 | 中 | 用 HashRouter 规避 |
| 6 | 8 场馆坐标需街道级核实（支持放大定位） | 中 | 开发时用高德/OSM 检索真实地址替换近似值 |
| 7 | 品牌图标：已由 `队徽.png` 生成 favicon 系列与 `brand/duihui.png` | 低 | 站点品牌图形统一用队徽；如需页脚用 `大礼堂-独立.svg` 可再处理 |
| 8 | OpenFreeMap 矢量瓦片为外网服务，国内访问情况需实测 | 中 | 上线前实测；如不可用换 MapTiler 免费层（需 key）或保留 positron 样式自托管 |
| 9 | MVP 仅桌面版，移动端未覆盖 | 低 | 一期接受；二期再加响应式 |
| 10 | maplibre-gl 需要 `@maplibre/maplibre-gl` 样式/worker 正确处理（CSP/worker 加载） | 低 | 用默认 worker URL，或打包 worker 内联 |

## 10. 开发规范

- TypeScript strict 模式开启
- 组件优先函数组件 + Hooks，禁止类组件
- 样式一律 CSS Modules，不使用内联样式（动态值除外）
- 图片统一懒加载（`loading="lazy"`）
- 内容一律写在 `content/` 的 Markdown 文档里，禁止硬编码进组件
- 提交信息规范：`feat:` / `fix:` / `chore:` / `docs:`
