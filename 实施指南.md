# 实施指南（供下一个 agent 开发使用）

> 目标：让一个不熟悉本仓库的 agent 能按步骤把网站从零实现到可部署。
> 前置：**先读** `../AGENTS.md`、`设计方向.md`、`技术方案.md`、`数据模型.md`，再按本指南推进。
> 现状：`website/content/`、`website/public/`、`website/src/data/geo/` 已就绪，待脚手架与编码。

## 0. 开工前必知

- 所有内容在 `content/**/*.md`（Markdown + frontmatter），**禁止硬编码内容进组件**
- 图片一律 WebP ≤300KB、懒加载；资源已在 `public/images/venues/<id>/`
- 品牌图形统一用队徽 `/brand/duihui.png`；标题 **"铁军精神·驻地变迁"**
- 视觉走"红色文献·铁军档案"：米黄纸张底 `#F7F1E3`、朱砂红 `#B03A2E`、深红 `#7B241C`、金 `#C9A227`、墨色 `#2C2C2C`；标题思源宋体、正文思源黑体、引文/印章思源楷体
- MVP：仅桌面版；首页仅 Hero；集锦仅照片墙；弹幕用预置留言；成果页骨架

---

## Phase 0 · 脚手架与基础配置

1. 在 `website/` 执行：`npm create vite@latest . -- --template react-ts`（目录已有 content/public/src，Vite 会保留非冲突目录）
2. 安装依赖：
   ```bash
   npm install react-router-dom framer-motion yaml maplibre-gl yet-another-react-lightbox
   npm install @fontsource/noto-serif-sc @fontsource/noto-sans-sc @fontsource/noto-kai-sc
   ```
3. 清理模板多余文件（`App.css`、`assets/react.svg` 等）
4. 建目录骨架：
   ```
   src/pages/  src/components/{home,map,venue,gallery,guestbook,common}/  src/styles/  src/lib/  src/types/  src/hooks/
   ```
5. `vite.config.ts`：设 `base: '/<repo>/'`（或部署后定）；地图用 HashRouter 无需 SPA fallback 配置
6. **验证**：`npm run dev` 起服务，`npm run build`、`npm run lint`、`npm run typecheck` 均通过

## Phase 1 · 内容解析层（`src/types/content.ts` + `src/lib/content.ts`）

按 `数据模型.md` 定义接口与解析：
- 类型：`Venue`（含 `cover`）、`TimelinePeriod`、`HistoricalEvent`、`VisitRecord`
- 解析：`import.meta.glob('/content/**/*.md', { eager: true, query: '?raw' })` + `yaml`
- 事件：`events/*.md` 一馆一文件，正文按 `## 日期 · 标题` 二级标题切分（标题以 `·` 拆分 date/title）
- 导出：`venues`（按 displayOrder 排序）、`timeline`（按序）、`eventsByVenue`、`visits`、`testimonialTexts`
- 解析字段缺失/类型错误**抛错**（开发期暴露）
- **验证**：写临时页面打印 `venues.length === 8`、`timeline.length === 8`；建议用 Vitest 写解析/数据校验测试

## Phase 2 · 全局骨架（`模块文档/01`）

- `styles/theme.css`：CSS 变量（配色/间距/字体栈）+ 纸张纹理（`global.css` 加 noise/grain）
- 字体：入口 `import '@fontsource/noto-serif-sc/400.css'` 等按需字重
- `App.tsx`：路由表（`/`、`/map`、`/venues/:id`、`/gallery`、`/guestbook`、`/achievements`、`*` NotFound），除首页外 `React.lazy`；`AnimatePresence` 淡入淡出过渡
- `Navbar`：5 项导航 + 队徽 `/brand/duihui.png` + 站点名"铁军精神·驻地变迁"；`NavLink` 高亮
- `Footer`：团队信息 + 场馆官网链接（读 venues 数据）
- **验证**：5 项导航可用、路由切换淡入淡出、content 数据可加载

## Phase 3 · 首页（`模块文档/02`）

- 仅 Hero：深红渐变背景，左侧队徽、右侧主标题"铁军精神·驻地变迁" + 副标语"踏四省红色热土，传百载铁军薪火"，底部"进入地图"按钮 → `useNavigate('/map')`
- 进场：标题/副标语错峰淡入（Framer Motion）；"盖章"仪式感动效按 `设计方向.md` §7
- **验证**：打开 `/` 显示 Hero，点击按钮跳转地图

## Phase 4 · 交互地图页（`模块文档/03`，核心）

- `MapView`：原生 `maplibre-gl`，`style: 'https://tiles.openfreemap.org/styles/positron'`，`center/zoom` 初值中国，`fitBounds` 包 8 馆；卸载 `map.remove()`
- 主题配色：`map.setPaintProperty` 调底图（米黄底、弱化水体道路）
- 四省强调：`provinces.json` 作 GeoJSON source，`fill`（红 ~12%）+ `line`（深红描边）
- 场馆层：GeoJSON source 'venues' + `circle` 图层（红、按时期/高亮改 `circle-radius/color/opacity`）；脉冲 rAF
- 悬停卡：`map.on('mouseenter'/'click')` + `queryRenderedFeatures`；React 浮层用 `map.project(coords)` 定位，边缘翻转；内容含场馆名/时期/简介/cover/精神标签/"查看详情"
- 时间轴：Context 状态 `periodKey`；点击节点 `map.flyTo({ center, zoom })` + 高亮；侧栏显示 `summary/spiritNote`；读 `?period=` 初始定位
- 侧栏：8 场馆列表，点击定位 + 弹卡
- 飞线：相邻场馆贝塞尔弧线 LineString + `line` 图层动画 dash 或 rAF 光点；进页自动播一遍、滑块触发单段；可重播
- 弹幕：`DanmakuLayer` 浮层盖在地图容器上，`pointer-events:none`，角落开关默认关，数据 `testimonialTexts`
- **验证**：`模块文档/03` §15 验收清单全过；重点：可放大看场馆位置、`?period=yunling` 定位、飞线/弹幕

## Phase 5 · 场馆详情页（`模块文档/04`）

- `/venues/:id`：读 `venues` 与 `eventsByVenue[id]`；无效 id 兜底 404/跳首页
- 区块：头部（cover/名称/城市/时期/实地角标/地址/官网）→ 简介（正文）→ 历史事件（按 `##` 拆分渲染，日期徽标）→ 铁军精神（spiritNote + spiritTags 标签）→ 实地照片图集（仅 `isFieldVisited`，灯箱）
- 灯箱：封装 `common/Lightbox.tsx`（yet-another-react-lightbox），详情页与集锦共用
- 顶部"← 返回地图"；**不做**上一下页切换（MVP）
- **验证**：8 馆均可访问、内容完整、图集灯箱可用

## Phase 6 · 调研集锦页（`模块文档/05`）

- 仅照片墙：读 `visits` 的 images（或 venues images）合并；CSS `columns` 瀑布流 + `break-inside: avoid`
- 点击放大：共用 `Lightbox`
- **验证**：照片墙瀑布流无错列、灯箱可切换

## Phase 7 · 留言板 + 弹幕（`模块文档/06`）

- `/guestbook`：嵌入 Giscus；仓库未配置时渲染占位（"留言功能筹备中"）。配置参数集中一处便于后改
- 弹幕在 Phase 4 已做（预置留言）
- **验证**：未配置不报错；配置后（换参数）可加载

## Phase 8 · 成果页（`模块文档/07`）

- 骨架占位：分区标题（实践概况/调研报告/立项答辩/实践记录/团队成员）+ 空状态占位；有真实文件的挂链接
- **验证**：可访问、无报错

## Phase 9 · 部署 GitHub Pages

- 设 `vite.config.ts` 的 `base`（子路径部署）
- 推送仓库 → Settings → Pages → Source: `gh-pages`（或配置 GitHub Actions，见 `技术方案.md` §8）
- 用 **HashRouter**，规避 SPA fallback
- **验证**：线上访问全站、地图瓦片/图片加载正常

---

## 验收清单（上线前）

- [ ] `npm run build` / `lint` / `typecheck` 全过
- [ ] 6 页面导航/路由/淡入淡出正常
- [ ] 首页 Hero → 地图过渡；地图可放大看场馆、四省强调、时间轴联动、飞线、弹幕开关
- [ ] 8 个详情页内容完整、图集灯箱可用
- [ ] 集锦照片墙 + 灯箱
- [ ] 留言板占位/Giscus 正常
- [ ] 成果页骨架
- [ ] 桌面浏览无异常、首屏加载可接受（图片已压缩）

## 风险与注意

- OpenFreeMap 为外网瓦片服务：国内访问需实测；失败时给加载占位/提示，备选 MapTiler
- `content/**/*.md` 字段严格按 `数据模型.md`，改字段须同步文档与解析层
- 队徽 `brand/duihui.png` 无法自动校验图案，需人工确认
- 停翅港坐标（33.65, 119.8）为近似值，上线前最好人工核实
- 弹幕/留言数据若团队后续要求接真实评论（GitHub API），见 `技术方案.md` §6.1 二期方案
